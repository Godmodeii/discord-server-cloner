version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - PORT=5000
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    volumes:
      - .:/app
    restart: unless-stopped
